version: 2

models:
  - name: int_cases_per_product
    columns:
      - name: product_id
        tests:
          - unique
          - not_null
  - name: int_accounts_with_countries
    columns:
      - name: account_id
        tests:
          - unique
          - not_null
